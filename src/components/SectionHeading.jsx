import React from 'react'

const SectionHeading = ({ text }) => {
  return (
    <div className="section-heading">
      <div className="section-heading-text">{text}</div>
      {/* <div className="section-heading-underline"></div> */}
    </div>
  )
}

export default SectionHeading
